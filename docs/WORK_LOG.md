# Work Log

## v1 site build
- Built 4-page static site (index/about/services/contact), shared header/footer, hand-written CSS (no Bootstrap pulled in).
- Applied Mecodex brand colors/logo/favicons.
- Files: `website/*.html`, `website/css/style.css`, `website/js/main.js`

## v2 premium art-direction pass
- Rewrote `website/css/style.css` on a proper design-token system: clamp() type scale (`--text-display/h1/h2/h3/body-lg/...`), full neutral surface/border/text scale, corner-bracket + ghost-numeral motifs, restrained shadow/motion policy. No new libraries, no ecommerce markup — CSS/JS stay hand-written.
- Redesigned the homepage hero (`website/index.html`): asymmetric layout with a vertical "Studio / 01" index rail, gradient-highlighted headline, enlarged network SVG with a slow rotating orbit ring + gentle drift (all disabled under `prefers-reduced-motion`), stat bar moved to a full-width strip under the fold.
- Gave each homepage section its own identity instead of repeating the same card grid: services overview → editorial numbered rows with hover reveal (`.service-rows`), Why MeCodex → asymmetric intro+callout / thin list (`.why-grid`), Process → offset alternating timeline, CTA → two-column bracket-framed panel (`.cta-panel`) reused on about.html and services.html.
- About page: values grid replaced with the same editorial row pattern (`.values-list`) for cross-page consistency.
- Services page: each of the 4 offerings now carries a large ghost numeral (01–04) and alternating column order for editorial rhythm; engagement-model cards got a short label tag.
- Header: added scroll state (`.is-scrolled`, toggled in `main.js`) — translucent at rest, solid/bordered + slightly shorter after 8px of scroll. Nav underline now animates on hover, not just on the active page.
- Contact page: applied the new `.text-link` underline-draw utility to the email link; layout/typography brought onto the same tokens, no structural change needed otherwise.
- Removed dead/unused utility classes (`.grid-2/-3/-4`) left over from v1 that nothing referenced after the section rewrites; kept `.text-link` by giving it one real use.
- Verified: CSS brace balance, HTML tag balance, internal link/anchor resolution, and used-class ↔ defined-class cross-check all pass across all 4 pages. Accessibility carried forward and re-checked: skip link, `aria-current="page"` per nav, form `label[for]`, alt text on logo, focus-visible outlines — all preserved.
- Reused from Ecomus (`ecomus-package/`): none directly copied — only used as a general reference for spacing/motion feel per the brief; no markup, CSS, or JS pulled in.
- Files touched: `website/index.html`, `website/about.html`, `website/services.html`, `website/contact.html`, `website/css/style.css`, `website/js/main.js`.

## v3 enterprise-elevation pass (visual/art-direction only — no new content)
- Typography: `h1`/`h2` bumped to weight 700 with tighter tracking; eyebrows tightened to `0.19em` tracking with explicit 600 weight. `h3`/`h4` untouched to preserve hierarchy.
- Depth/texture system (`website/css/style.css`): global background grid upgraded to a two-tier (64px + 128px) blueprint grid; added a reusable `.grid-plane` utility; hero visual got a layered radial vignette + finer grid behind the network SVG, small corner brackets, a very low-opacity scanline sweep (respects `prefers-reduced-motion`), and a mono "readout" line reporting the diagram's own literal node/link counts (not a product metric).
- Corner-bracket motif (`.bracket-frame`, pre-existing) reused on `.engagement-card` (services.html) and `.contact-form` (contact.html) so it reads as a system, not a one-off.
- Header nav: added a CSS-only (`:hover`/`:focus-within`, no JS) Services mega-menu preview — 4 offerings with existing icons, linking to `services.html#anchor`. Desktop-only; `display:none` below 860px so mobile nav is unaffected and the plain Services link still works everywhere. Added to all 4 pages' header nav (footer nav untouched).
- Footer: added a 2px gradient top-border accent and a small mono wordmark/status line (`// MeCodex — Engineering Studio`) next to the copyright — no new links or claims.
- No new libraries, no ecommerce patterns, brand colors/logo untouched. Removed a duplicate `.footer-bottom` rule found in the 640px breakpoint block while editing (pre-existing, harmless, cleaned up in passing).
- Verified: CSS brace balance (285/285), HTML tag balance (div/section/nav/ul/li/a/form/header/footer/main) across all 4 pages, every class used in HTML has a definition in `style.css`, all same-page and cross-page `#anchor` links resolve. Live-browser rendering was not available in this session's tooling (static-snapshot preview only, no stylesheet actually loaded) — carries forward as the existing "Next" QA item.
- Files touched: `website/css/style.css`, `website/index.html`, `website/about.html`, `website/services.html`, `website/contact.html`, `docs/DESIGN_SYSTEM.md`.

## v4 motion pass (client asked for "more animation" as a direct follow-up to v3)
- Header: added a `.scroll-progress` accent bar (2px gradient, fills with scroll depth), driven by `main.js`'s existing scroll handler. Added to all 4 pages' header markup.
- Hero: staggered load-in sequence (`heroEnter` keyframes, no JS) for eyebrow/H1/lead/CTAs/visual/index rail. Hero stat numbers (`.hero-statbar`) now count up via a small `requestAnimationFrame` counter in `main.js` when scrolled into view — only purely-numeric stats count (plain digits or digits+`%`); `24/7` and `3 yrs+`-style stats fade/rise instead of being mis-counted. Two small SVG "data packet" dots now travel along the network diagram's connection lines via CSS `offset-path`, reinforcing the existing orbit/drift/scanline motif.
- Scroll-reveal system made richer without new JS: `.service-row`, `.why-item`, `.value-row` items now stagger in individually (nth-child `transition-delay`) once their parent list is visible, instead of the whole list fading as one block. `.process-step` markers pop in and connecting lines draw in, staggered per step. Sibling `.reveal` pairs inside `.grid`/`.why-grid` (story-grid, contact-grid, why-grid, engagement-grid) get a small stagger too.
- Nav mega-menu: open/close transition slowed and softened (fade+rise instead of an instant-feeling fast fade), its 4 items stagger in via nth-child delay — still pure CSS, still desktop-only, mobile drawer untouched.
- Hover polish: service-row and nav-menu-item icons get a small rotate/scale on hover; `.engagement-card` gets a hover-only tinted glow shadow (no resting shadow, keeps v3's restrained-shadow rule); `.btn-primary` shifts its gradient's background-position on hover for a touch more personality (no rainbow cycling).
- `prefers-reduced-motion: reduce` extended to cover every new effect above (hero entrance, stat counter — JS itself skips the RAF loop, stagger transforms, mega-menu, icon hover, packet dots, button gradient shift) — content shows in its final state instantly, matching the pre-existing rule for `.reveal` and the hero orbit/drift/scanline.
- No new libraries. No new sections, claims, colors, or content — motion/interaction only, same anti-pattern list as v2/v3.
- Verified: CSS brace balance (357/357), HTML tag balance (div/section/nav/ul/li/a/form/header/footer/main/p/svg/g) across all 4 pages, every class used in HTML resolves in `style.css`, all same-page and cross-page `#anchor` links resolve, `main.js` passes `node --check`. Live-browser rendering of local files remained unavailable in this session's tooling (same limitation as v3); verification was via static markup/CSS/JS analysis — carries forward as the existing "Next" QA item.
- Files touched: `website/css/style.css`, `website/js/main.js`, `website/index.html`, `website/about.html`, `website/services.html`, `website/contact.html`, `docs/DESIGN_SYSTEM.md`.

## v6 particle background + number flip (two specific client requests)
- Client requests (translated): "I want dots moving randomly in the background" and "numbers should animate — spinning/flipping/tumbling" (بتلف بتتشقلب — odometer/flip-counter style, explicitly not a plain count-up).
- Ambient particle field: added `<canvas class="bg-particles" aria-hidden="true">` to all 4 HTML files (right after the skip link) and a new `main.js` §9 — vanilla `requestAnimationFrame` particle system, no library. 16/26/42 dots depending on viewport width (<640/<1024/≥1024), teal/blue low-opacity, drift + wrap at edges. Fixed full-viewport canvas, `z-index: -1`, `pointer-events: none`, sits behind all content and the existing background grid/glow without affecting layout. Pauses on `visibilitychange` (tab hidden), redraws (debounced) on resize, and never starts its loop under `prefers-reduced-motion: reduce` (draws one static frame instead).
- Number-flip system: hero stat counters (`.hero-statbar .stat-num`) now digit-roll like an odometer/split-flap counter instead of a smooth linear count-up — `main.js` §6 rewritten to split the value into per-digit spans and step through ~10 discrete values, retriggering a `digitFlip` `rotateX` keyframe on each digit that changes. Ghost numerals (services.html `.service-detail-ghost`) get a `rotateX` flip-in (`ghostTumbleIn`) on first scroll-reveal, then hand off to the existing v5 `ghostBreathe` ambient loop. Process step markers (`.step-index`), service-row/why-item/value-row index numerals (`.row-index`, `.feature-num`, `.value-mark`) all gained a `rotateX` flip/rotate entrance timed with their row's existing stagger delay, replacing plain fades/scale-pops. Added `perspective` to each numeral's containing block so the flips read as real 3D, not a flat squish.
- `prefers-reduced-motion: reduce` extended: particle loop never starts (static frame only); every new flip/tumble/digit-roll collapses to instant/fade, no `rotateX`, no digit-span construction (JS-gated for the stat counter, CSS-gated with explicit overrides for the rest).
- No new libraries. Particle system lives in `main.js` (not a separate file) — small enough (~100 lines) to stay inside the existing single-file convention per `AI_INSTRUCTIONS.md`.
- Verified: `node --check` on `main.js` passes; CSS brace balance (0/0 net); every class used across the 4 HTML files resolves in `style.css`; all same-page/cross-page `#anchor` links resolve. Live-browser rendering of local `file://` paths remained unavailable in this session's tooling (same limitation noted in v3–v5) — carries forward as the existing "Next" QA item.
- Files touched: `website/css/style.css`, `website/js/main.js`, `website/index.html`, `website/about.html`, `website/services.html`, `website/contact.html`, `docs/DESIGN_SYSTEM.md`.

## v5 full motion sweep (client escalation: "animation in every corner")
- Generalized the section-heading reveal (`.section-head`, `.why-intro`) so eyebrow/heading/lead/callout cascade in individually instead of one flat block — closes the "section headings just fade as a block" gap and, as a side effect, gives about/services/contact's first screen the same staggered "arrival" the homepage hero already had.
- Headline word-reveal: hero H1 + each page's H1 wrap into `.word` spans (`main.js`, DOM-safe around the hero's nested gradient span) that cascade in via `--i` transition-delay, layered on top of the block-level rise.
- Ambient always-on motion: slow background glow drift (`bgDrift`, body::before), eyebrow-dot pulse (`eyebrowPulse`, sitewide), services-page ghost-numeral breathe (`ghostBreathe`) — all transform/opacity-only, all bounded/low-count.
- Cursor-aware spotlight: pointer-following radial highlight on `.service-row` / `.value-row` / `.why-item` / `.contact-info-item`, via rAF-throttled mousemove setting `--mx`/`--my`; scoped to `pointer: fine` in both JS (matchMedia check) and CSS (media query), so touch is unaffected.
- Footer columns (`.footer-top`) now reveal-in on scroll via the existing `.reveal` pattern (previously static).
- Contact form: submit button gets a brief loading-spinner state before the success message appears; success message now scales/fades in and its checkmark draws via `stroke-dashoffset` instead of popping in; fields get a `:focus-within` label tint + focus lift.
- Micro-interaction sweep: logo hover scale, footer/social icon hover rotate+scale, tech-stack chips (about.html) now stagger in on scroll and gained a hover tint, service-detail icons (services.html) get the existing icon hover-rotate treatment, contact-info icon chips get hover scale, `.engagement-card` hover shadow now transitions instead of snapping, `.contact-info-item a` color hover (previously untransitioned) fixed.
- `prefers-reduced-motion: reduce` extended with a dedicated v5 block covering every new keyframe/animation/stagger added in this pass (`bgDrift`, `eyebrowPulse`, `ghostBreathe`, `btnSpin`, `checkDraw`, all new stagger transitions, the spotlight transition, the word-reveal words).
- No new libraries, no new sections/claims/colors. Hand-written CSS/JS only, stayed inside `website/css/style.css` and `website/js/main.js` (plus the 4 HTML files for new classes: `word-reveal` on H1s, `reveal` on `.footer-top`).
- Verified: CSS brace balance (431/431), HTML tag balance across all 4 pages, every HTML class resolves in `style.css`, all same-page/cross-page anchors resolve, `main.js` passes `node --check`, no console errors on any of the 4 pages in a browser preview (live-render still limited to a static file:// snapshot in this session's tooling, same limitation as v3/v4 — carries forward as the existing "Next" QA item).
- Files touched: `website/css/style.css`, `website/js/main.js`, `website/index.html`, `website/about.html`, `website/services.html`, `website/contact.html`, `docs/DESIGN_SYSTEM.md`.

## v7 interaction/parallax pass (client: "continue, add more animation/detail")
- Tilt: subtle 3D tilt (`rotateX`/`rotateY`, `perspective()`) on `.service-row`/`.why-item`/`.value-row` (max ~4°, wide rows) and `.engagement-card` (max ~7°, narrower cards), driven by `--tilt-x`/`--tilt-y` custom properties. Magnetic pull on `.btn-primary`/`.btn-secondary` (a few px toward the cursor, `--mag-x`/`--mag-y`). Both `pointer: fine`-gated in JS and CSS; snap back on `mouseleave` via the existing `transform` transition.
- Parallax: gentle scroll-linked `translateY` (capped 16px) on `.hero-visual-frame` and (capped 10px) on `.bg-particles`, computed inside the existing header/progress-bar scroll handler — no second scroll listener. Applied to `.hero-visual-frame` rather than `.hero-visual` itself, since the latter still owns the v4 `heroEnter` load-in animation on `transform` and would fight an inline transform for the same property.
- Section divider line-draw: new `.section-divider` (brand-gradient 1px rule, `scaleX(0→1)`) between major sections on index/about/services (3 each) — reuses the existing `.reveal`/IntersectionObserver system, no new observer.
- Icon stroke-draw: homepage `.service-row` and services-page `.service-detail` card icons now draw in (`stroke-dasharray`/`stroke-dashoffset`) on their existing reveal state instead of just fading — CSS-only, no new JS or HTML.
- Form-submit success burst: a single soft teal ring (`successPulse`, ~900ms) expands from the checkmark once `.form-success.is-shown` is set — reuses the class main.js already toggles, no new JS.
- Nav underline gradient sweep: `.primary-nav a::after` now shifts `background-position` on hover instead of sitting as a flat two-color fill (same technique as `.btn-primary`'s existing hover). Logo idle-pulse skipped (optional/lowest priority, deemed unnecessary risk to the logo markup).
- Scroll-to-top button: new `.scroll-top`, fades in past ~60% of viewport height scrolled, smooth-scrolls to top on click, toggled by the same consolidated scroll handler.
- Listener consolidation: hoisted a single `reducedMotion` snapshot to the top of `main.js` (removed 3 duplicate `matchMedia` re-declarations in §5/§6/§9). Scroll: still exactly one `scroll` listener (rAF-throttled), now also driving parallax + scroll-to-top in addition to header state/progress bar. Pointer effects (spotlight/tilt/magnetic) share one Map-based rAF batch (`flushPointerFX`) instead of one rAF token per effect — still one `mousemove` listener per interactive element (as v5's spotlight already was), just funnelled through a single shared flush. rAF loops: still exactly one continuous loop (particle canvas); everything else is one-shot rAF batching, not a loop.
- `prefers-reduced-motion: reduce` extended: tilt/magnetic never attach in JS (gated) and get a defensive `transform: none !important` in CSS; scroll parallax never writes a transform; divider lines/icon strokes show fully drawn; success burst ring disabled (checkmark's own fade already covered).
- Verified: CSS brace balance (495/495), HTML tag balance across all 4 pages (custom stack-based checker, not just tag-count), every class used in HTML/JS resolves in `style.css`, all same-page/cross-page anchors resolve, `main.js` passes `node --check`. Live-browser check this pass: served `website/` over a local static HTTP server (file:// preview in this session's tooling only renders a static snapshot) and exercised hover/scroll/tilt/scroll-to-top/contact-form-success in a real browser — no console errors on index/services/about/contact.
- Files touched: `website/css/style.css`, `website/js/main.js`, all 4 HTML files (scroll-to-top button; section dividers on index/about/services; `.success-icon-wrap` span in contact.html), `docs/DESIGN_SYSTEM.md`.

## v8 — new pages + sections
- New pages built to the existing v7 design system (shared header/nav/footer/particle-canvas/scroll-progress, same tokens/components, no parallel visual language): `website/faq.html` (9 real Q&A in the new Accordion component), `website/portfolio.html` (nav label "Work"; honest "case studies coming soon" — 3 placeholder `.engagement-card` slots, no fabricated client names/logos), `website/careers.html` (culture/values reusing `.values-list` pattern + honest "no open roles, send your CV" CTA, no fake job listings/salaries), `website/insights.html` (honest "blog coming soon" content hub + front-end-only "notify me" form, no fake post titles/dates/authors).
- New component: **Accordion** (`.accordion`/`.accordion-item`/`.accordion-trigger`/`.accordion-panel`), styled as a hairline-row variant consistent with `.service-row`/`.value-row`. Real `<button>` per question (`aria-expanded`/`aria-controls`), `role="region"`/`aria-labelledby` panel, capped `max-height` transition (not the newer CSS grid-rows trick — chosen for cross-browser reliability), reuses the existing `.reveal` IntersectionObserver for entrance stagger. JS module added: `main.js` §10 (toggles `aria-expanded` + an `.is-open` class per item — not a sibling combinator off the button, since the button is nested in `.accordion-heading`). Documented in `docs/DESIGN_SYSTEM.md`.
- `index.html`: new FAQ section before the CTA panel — 5-item accordion subset of faq.html's content + "See All FAQs" button link to faq.html.
- `about.html`: Tech Stack section elevated from a flat chip grid into its own `.section-head` + grouped `.stack-groups` (Frontend / Backend / Infrastructure / Tools & integrations), same chips/hover/stagger, just organized and given real section presence.
- `insights.html` newsletter notice: front-end only, `main.js` §11 just prevents the default GET submission (no fake success state, matching the contact form's own honesty pattern).
- Nav decision: primary header nav held at 5 items (Home/About/Services mega-menu/Work/Contact) to avoid overcrowding; FAQ/Careers/Insights added to a new footer "Company" column on all 8 pages (replacing the old footer "Services" quick-link column — those 4 anchors remain one hover away via the header mega-menu everywhere). `aria-current="page"` set correctly per page across header + footer.
- Verified across all 8 pages: CSS brace balance (535/535), every HTML class resolves in `style.css`, every internal href/anchor resolves (including cross-page anchors like `about.html#stack` and `faq.html`), no duplicate `id`s, `main.js` passes `node --check`, `prefers-reduced-motion` audited to cover the new accordion's open/close + entrance stagger.
- Files touched: `website/index.html`, `website/about.html`, `website/services.html`, `website/contact.html` (nav/footer restructure + FAQ section + stack elevation), `website/css/style.css`, `website/js/main.js`. Files created: `website/faq.html`, `website/portfolio.html`, `website/careers.html`, `website/insights.html`. Docs updated: `PROJECT_CONTEXT.md`, `DESIGN_SYSTEM.md`, `TASKS.md`.

## Docs accuracy review (2026-08-27)
- Independent audit pass: read every project .md (README, AI_INSTRUCTIONS, DESIGN_SYSTEM, PROJECT_CONTEXT, TASKS, WORK_LOG) against the actual `website/` files — no code changes made, docs-only check.
- Verified against real files: page count/names (8 pages + 404, matches README/PROJECT_CONTEXT), `portfolio.html` has 6 `.engagement-card` entries, `careers.html` has 3 accordion job listings, `insights.html` has 3 `.insight-article` blocks (all matching TASKS.md/PROJECT_CONTEXT.md v9 claims). `og:url`/`robots.txt`/`sitemap.xml` all point to `https://mostafaessam7.github.io/MeCodex/` (not placeholder `mecodex.com`), sitemap lists all 8 real pages, no `CNAME` file present — matches the "no custom domain yet" status in TASKS.md. Footer LinkedIn/X links are still literal `href="#"` on every page, and `contact.html`'s form has no `action`/Formspree endpoint — both confirmed still-pending exactly as TASKS.md already stated. `js/main.js` passes `node --check`; its `§0–§10` section comments match the DESIGN_SYSTEM.md motion-system writeup.
- Conclusion: docs were already accurate and current, no stale/invented progress found. No edits needed to README.md, AI_INSTRUCTIONS.md, DESIGN_SYSTEM.md, PROJECT_CONTEXT.md, or TASKS.md — this entry is the only change from this pass.

## v9 — content fill + new home sections (client: "fill them" [portfolio/careers/insights] + "add more sections to the home page")
- `portfolio.html`: replaced the 3 "coming soon" placeholder slots with 6 illustrative project cards (`.engagement-card`/`.bracket-frame`, reused as-is), each with a title, 1–2 sentence description, and a small tag row (new `.eng-tag`/`.eng-tags`, a smaller non-interactive variant of the existing `.stack-chip` pill token). A framing line at the top of the section states plainly these are representative/illustrative examples of the kind of work MeCodex does, not published case studies from named clients — no invented client names, logos, or testimonial quotes anywhere. CTA panel copy updated to match (no longer "coming soon").
- `careers.html`: added an "Open roles" section with 3 real generic listings (Senior Full-Stack Engineer, Product Designer, DevOps/Platform Engineer) in the existing Accordion component — each with type, "Remote-first / Cairo, Egypt" location, a "what you'll do" / "what we're looking for" list (reusing `.service-detail-list` from services.html), and a `mailto:hello@mecodex.com` apply link with a role-specific subject line. No fabricated salaries, headcount, or deadlines. Kept the "don't see your role? send your CV anyway" fallback CTA, copy adjusted since the page is no longer "not hiring."
- `insights.html`: replaced the "blog coming soon" placeholder with 3 full, original articles written directly on the page (`.insight-article`) under a "MeCodex Engineering" byline with plausible 2026 dates — "How We Scope a Software Project in the First Two Weeks," "TypeScript in Production: Our Default Choices," and "What 'Senior-Led Delivery' Actually Means Day to Day." Chose full on-page content over separate article pages/files (fewer new files, fewer links to verify, per the brief's stated preference). Kept the existing front-end-only "notify me" form, copy tweaked slightly.
- `index.html`: added two new sections, each with a full `.section-head` and participating in the existing `.reveal`/section-divider system — **Featured Work** (after Process, before FAQ): 3 of the portfolio cards, content copied verbatim from `portfolio.html`, "View All Work" link; **From the Blog** (after FAQ, before the final CTA): 3 article previews (title/date/excerpt) linking to the new anchors on `insights.html`, "Read More Insights" link. Only one `.cta-panel` remains on the page (unchanged, at the bottom).
- New CSS (`website/css/style.css`, one new block, §16g): `.eng-tags`/`.eng-tag` (card tag chips), `.role-title`/`.role-meta` (job-listing accordion trigger), `.accordion-panel-inner h4`/`.service-detail-list` overrides + `.role-apply` (job-listing panel content), `.insight-meta`/`.insight-article` (article meta line + paragraph spacing, since the base `p` rule is intentionally margin-less elsewhere), `code`/`.engagement-card code` (inline code styling, not previously needed anywhere on the site), and `.section-more` (alias of the existing `.accordion-more` link-row layout, reused outside accordion contexts for the two new home "view more" links).
- Bug caught and fixed during this pass: a `sed` replace-all while simplifying a new link's class list accidentally stripped the `row-link` class off the 4 pre-existing homepage service-row "Learn more" links too; restored before finishing.
- Verified: CSS brace balance (549/549), `main.js` unchanged this pass (`node --check` still passes), every internal `href`/anchor across all 8 pages resolves (scripted check), no duplicate `id`s on the touched pages, HTML tag-count balance on the 4 touched pages. Live-browser rendering remained unavailable in this session's tooling (same file:// / localhost-policy limitation as prior passes) — carries forward as the existing "Next" QA item.
- Files touched: `website/index.html`, `website/portfolio.html`, `website/careers.html`, `website/insights.html`, `website/css/style.css`. Docs updated: `PROJECT_CONTEXT.md`, `DESIGN_SYSTEM.md`, `TASKS.md`.
